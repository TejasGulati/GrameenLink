{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "src": "/GrameenLink/(.*)", "dest": "/GrameenLink/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}